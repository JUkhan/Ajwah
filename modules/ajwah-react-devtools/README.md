# ajwah-react-devtools

```js
import { devTools } from 'ajwah-react-devtools';


setStoreContext({
    states: [CounterSate, TutorialState],
    effects: [CounterEffect],
    devTools: devTools({ maxAge: 10 })
});

```
